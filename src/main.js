import './app.css'
import App from './routes/login/index.svelte'

const app = new App({
  target: document.getElementById('app'),
})

export default app

/** App.svelte was removed as file was not compiling with it*/
